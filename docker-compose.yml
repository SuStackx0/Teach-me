services:
  teachme:
    build: ./app
    ports:
      - "8001:8001"
    volumes:
      - ./.teach:/app/.teach
    environment:
      - TEACH_DATA_DIR=/app/.teach
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    restart: unless-stopped
